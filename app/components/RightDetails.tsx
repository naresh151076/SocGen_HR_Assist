"use client";
import { ChevronRight, FileCheck2, ShieldCheck, X } from "lucide-react";
import { scenario } from "../data/scenario";
export function RightDetails({open,onClose}:{open:boolean;onClose:()=>void}){ return <aside className={`${open?"w-[330px] border-l":"w-0 border-l-0"} shrink-0 overflow-hidden border-zinc-200 bg-white transition-[width] duration-200`} aria-label="Request details"><div className="w-[330px] p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--sg-red)]">Live context</p><h2 className="mt-1 text-xl font-bold">Request details</h2></div><button onClick={onClose} aria-label="Close details" className="rounded-lg p-2 hover:bg-zinc-100"><X size={19}/></button></div><p className="mt-5 text-sm text-zinc-600">This panel holds supporting details when they are needed, without pulling Radu away from the operation.</p><div className="mt-6 rounded-xl bg-zinc-50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-zinc-500">Current scenario</p><p className="mt-2 font-bold">{scenario.course}</p><p className="mt-1 text-sm text-zinc-600">12 requested · Paris · {scenario.trainer}</p></div><div className="mt-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-zinc-500">Control evidence</p>{scenario.checks.map(c=><div key={c} className="flex gap-2 border-b border-zinc-100 py-3 text-sm"><ShieldCheck className="shrink-0 text-emerald-600" size={16}/><span>{c}</span></div>)}</div><button className="mt-6 flex items-center gap-2 text-sm font-bold text-[var(--sg-red)]"><FileCheck2 size={16}/> View full audit trail <ChevronRight size={15}/></button></div></aside> }
