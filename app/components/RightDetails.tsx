"use client";
import { FileCheck2, ShieldCheck, X } from "lucide-react";
import type { ConversationResource } from "../data/conversations";
export function RightDetails({open,onClose,resource}:{open:boolean;onClose:()=>void;resource:ConversationResource|null}){ if(!resource) return null; return <aside className={`${open?"w-[330px] border-l":"w-0 border-l-0"} h-full min-h-0 shrink-0 overflow-hidden border-zinc-200 bg-white transition-[width] duration-200 ease-out motion-reduce:transition-none`} aria-label="Supporting details"><div className="scrollbar h-full w-[330px] overflow-y-auto p-5"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--sg-red)]">{resource.kind}</p><h2 className="mt-1 text-xl font-bold">{resource.title}</h2></div><button onClick={onClose} aria-label="Close details" className="rounded-lg p-2 hover:bg-zinc-100"><X size={19}/></button></div><p className="mt-5 text-sm leading-6 text-zinc-600">{resource.summary}</p><div className="mt-6 rounded-xl bg-zinc-50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-zinc-500">Document summary</p>{resource.facts.map(fact=><div key={fact.label} className="mt-3"><p className="text-xs font-bold uppercase tracking-wide text-zinc-500">{fact.label}</p><p className="mt-0.5 text-sm font-semibold">{fact.value}</p></div>)}</div><div className="mt-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-zinc-500">Evidence retained</p>{resource.evidence.map(item=><div key={item} className="flex gap-2 border-b border-zinc-100 py-3 text-sm"><ShieldCheck className="shrink-0 text-emerald-600" size={16}/><span>{item}</span></div>)}</div><p className="mt-6 flex items-center gap-2 text-sm font-bold text-[var(--sg-red)]"><FileCheck2 size={16}/> Fictional demo document</p></div></aside> }
